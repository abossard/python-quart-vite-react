import React from 'react'
import ReactDOM from 'react-dom/client'
import TicketConsole from './TicketConsole.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TicketConsole />
  </React.StrictMode>,
)
